/*
Suponiendo que no existe la instrucción MUL, crear un algoritmo que reciba en EBX y ECX los
dos valores y retorne en EAX su producto.

*/

// agregar mayor eficiencia buscando el menor valor de ambos

	MOV EDX,DS
	MOV EAX,0x01
	LDL ECX,1
	LDH ECX,4
	SYS 1
	MOV EBX,EDX

	MOV EDX,DS
	MOV EAX,0x01
	LDL ECX,1
	LDH ECX,4
	SYS 1
	MOV ECX,EDX
	MOV EAX,0
bucle:
	CMP [EBX],0
	JZ fin
	ADD EAX,[ECX]
	SUB [EBX],1
	JMP bucle

fin:
	stop